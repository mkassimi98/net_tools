# Optional local overrides for your machine.
# Copy to local.env and change values as needed.

# VPN_INTERFACE=tun0
# VPN_INTERFACE=wg0

# VPN_PING_TARGET=10.8.0.1
# VPN_PING_COUNT=4

# SSH target reachable through the VPN (for vpn-reachability demo).
# VPN_SSH_TARGET=10.8.0.50
