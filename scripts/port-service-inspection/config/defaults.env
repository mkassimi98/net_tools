# Shared defaults for port-service-inspection scripts (bash + python)
# CLI args still override these values.

SERVICE_NAME=ssh

PORT_TARGET=22

PORT_STATE_OPEN_HOST=127.0.0.1
PORT_STATE_OPEN_PORT=22

PORT_STATE_CLOSED_HOST=127.0.0.1
PORT_STATE_CLOSED_PORT=9
PORT_STATE_CLOSED_TIMEOUT_SECONDS=2

PORT_STATE_DOWN_HOST=203.0.113.10
PORT_STATE_DOWN_PORT=80
PORT_STATE_DOWN_TIMEOUT_SECONDS=2

FIREWALL_PORT=22
